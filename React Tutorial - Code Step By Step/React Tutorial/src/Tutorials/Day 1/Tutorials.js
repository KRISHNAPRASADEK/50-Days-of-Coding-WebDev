import React from "react";

function Tutorials() {
  return (
    <div>
      <h1>Tutorials</h1>
    </div>
  );
}

// react is a javascript library, not a framework, used for building fast single page applications (spa)

//compnent types - basically 2 types
//fn'al and class component

//in which other types of components
// Higher order,pure, controlled, uncontrolled components..

//component - is a reusable piece of code, like fn, but powerful than fn
// nested component - one component inside another component is possible

//  JSX- javascript xml - write html, javascript together
// use like html or xml tags
// we can use react without jsx, but it will be complex
// eg:      // return React.createElement("div","app",React.createElement("h1","heading","hello world"))

//   we can use angular and react together in a project

// React click event and function
// onClick={()=>alert("hello")} or {alertFunction} // here no paranthesis required, if added then fn will automatically called without clicking button

// React States can be used in fnal and class components

// state with fnal component
export default Tutorials;
